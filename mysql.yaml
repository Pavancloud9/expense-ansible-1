- name: mysql
  hosts: mysql
  become: yes
  tasks:
  - name: install mysql-server
    ansible.builtin.package:
      name: mysql-server
      state: installed

  - name: start the mysql-server
    ansible.builtin.service:
      name: mysql-server
      state: started
      enabled: yes


  